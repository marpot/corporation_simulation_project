# Infrastructure

Docker Compose is used for local development. Kubernetes resources will be added after the application is working, and Helm will later package those Kubernetes resources.

Terraform is intentionally outside the current project scope.
