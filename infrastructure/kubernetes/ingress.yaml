apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: corporation-simulation
spec:
  ingressClassName: nginx
  rules:
    - host: corporation-sim.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  name: http
